============
Installation
============

At the command line::

    $ easy_install gigon-common

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv gigon-common
    $ pip install gigon-common
